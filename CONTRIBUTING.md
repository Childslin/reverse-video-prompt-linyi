# Contributing to reverse-video-prompt

欢迎提交复刻案例、失败诊断、成功经验和规则改进。

这个 skill 的目标不是收集更多单条提示词，而是把“为什么某条复刻成功/失败”提炼成可复用规则，帮助不同产品、不同视频模型、不同 TikTok/UGC 场景都能更稳。

## 推荐提交内容

你可以提交：

- 失败复刻案例：原片和生成片哪里不像
- 成功复刻案例：哪几个约束让结果明显变好
- 新产品品类的通用规则
- 音频、前三秒、物理正确性、产品卖点证明等方面的改进
- eval case，用来防止以后规则退化

## 最小信息

请尽量提供：

- 产品类别
- 目标平台或视频模型
- 原提示词
- 原片描述或你有权分享的链接
- 生成片描述或你有权分享的链接
- 你看到的具体问题
- 你认为可复用的规则

不要公开上传你没有权利分享的原始视频、人物隐私素材、平台下载素材或客户文件。可以用文字描述、打码截图、contact sheet、时间点说明替代。

## 什么样的改动更容易合并

好的改动通常是：

- 适用于多个产品，而不是只修一个视频
- 能被未来的视频/音频证据验证
- 告诉提示词作者具体应该怎么写
- 不会和已有的机位、音频、物理正确性规则冲突
- 足够短，不让主 skill 变成案例堆积

不建议合并的改动：

- 只针对一个产品的偶然细节
- 把某个用户的本地路径或私有素材写进 skill
- 只写“更真实”“更高级”但没有可执行细节
- 让提示词变得更长但没有解决新的失败模式

## 建议工作流

1. 先提交 issue，说明复刻失败或成功经验。
2. 用 `references/iteration-workflow.md` 的 learning card 格式提炼问题。
3. 如果是通用问题，再提交 PR。
4. PR 里说明：修了什么、为什么可复用、哪些细节没有加入以避免过拟合。

## 半自动提交

仓库可以使用 `scripts/export_iteration_case.py` 把一次复刻反馈整理成 GitHub issue。

默认情况下脚本只在本地生成文件，不上传素材、不上传使用记录。只有在你明确传入：

```bash
--upload issue --yes --repo owner/repo
```

时，脚本才会调用 GitHub CLI 创建 issue。

原始视频不会作为附件自动上传。请优先使用授权链接、文字描述、打码截图或 contact sheet。

## PR Checklist

- [ ] 我说明了这个失败/成功案例的产品类别和目标场景。
- [ ] 我说明了这条规则为什么能跨产品复用。
- [ ] 我没有提交无授权原始视频或隐私素材。
- [ ] 我没有把单个案例的偶然细节写进主流程。
- [ ] 如果适合，我添加或更新了 eval case。
